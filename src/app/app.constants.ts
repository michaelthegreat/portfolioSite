export enum APP_PATHS_ENUM {
    ROOT = '/',
    HOME = '',
    CONTACT = 'contact',
}
export const DEFAULT_MAX_MESSAGE_LENGTH = 1024;
export const DEFAULT_MAX_VARCHAR_LENGTH = 256;
export const NETWORK_ERROR_DEFAULT_MESSAGE = 'Error during data fetch has occurred';